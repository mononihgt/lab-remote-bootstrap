# Required
CLOUD_USER=your_cloud_user
CLOUD_HOST=your_cloud_host

# Optional: cloud ssh port
CLOUD_SSH_PORT=22

# Optional: reverse ssh port opened on cloud host
REVERSE_PORT=2223

# Optional: target local ssh port (usually 22)
LOCAL_SSH_PORT=22

# Optional: who will login via tunnel on lab host
TARGET_USER=$USER
TARGET_HOME=$HOME

# Optional: install location on host
INSTALL_ROOT=/opt/lab-remote-stack

# Optional: source files
SSH_KEY_SOURCE=$HOME/.ssh/id_ed25519_autossh
# Optional: override clash source dir (supports relative path, default is repo ./assets/clash)
# CLASH_SOURCE_DIR=assets/clash

# Optional: clash ports and timezone
CLASH_HTTP_PORT=7890
CLASH_SOCKS_PORT=7891
CLASH_API_PORT=9090
HOST_TIMEZONE=Asia/Shanghai
