# Clash Configuration - Minimal Template
# Auto-generated at {{generated_time}}
# Subscription: {{subscription_name}}

port: 7890
socks-port: 7891
allow-lan: true
mode: rule
log-level: info
external-controller: 127.0.0.1:9090

proxies:
{{proxies}}

proxy-groups:
  - name: "PROXY"
    type: select
    proxies:
{{proxy_names}}
      - DIRECT

rules:
  # China direct
  - GEOIP,CN,DIRECT

  # Default proxy
  - MATCH,PROXY
