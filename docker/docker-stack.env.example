# Required
CLOUD_USER=your_cloud_user
CLOUD_HOST=your_cloud_host

# Optional: cloud ssh port
CLOUD_SSH_PORT=22

# Optional: reverse ssh port opened on cloud host
REVERSE_PORT=2223

# Optional: keep these paths if you want defaults
SSH_KEY_SOURCE=$HOME/.ssh/id_ed25519_autossh
# Optional: override clash source dir (supports relative path, default is repo ./assets/clash)
# CLASH_SOURCE_DIR=assets/clash
PROJECT_ROOT=$HOME/my-docker-lab
WORKSPACE_HOST_DIR=$HOME/my_project

# Optional: docker names
IMAGE_NAME=my-lab-cpu
CONTAINER_NAME=lab-cpu-container

# Optional: container root password and timezone
ROOT_PASSWORD=change_me
CONTAINER_TIMEZONE=Asia/Shanghai

# Optional: clash ports in container (will also be exported in zshrc/profile)
CLASH_HTTP_PORT=7890
CLASH_SOCKS_PORT=7891
CLASH_API_PORT=9090
